# Stephanie Byerly Crowned Story

Public route: https://www.fordivine.com/crowned-stories/stephanie-byerly

Build with `next build --webpack`, then run `node scripts/finalize-static.mjs`.
Deploy `out/index.html` and its hashed story-interactions script to `fordivine-upload-to-github/crowned-stories/stephanie-byerly/`. Image and font assets live in that route directory. CSS is inlined. Do not copy the private preview’s robots.txt or vercel.json into production.

Published at the user's explicit request on September 21, 2026. The private preview remains separate. Prelaunch source and interaction checks passed; browser visual verification was unavailable.
